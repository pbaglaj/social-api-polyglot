Pliki haseł używane jako Docker Compose secrets.

Format: jeden sekret = jeden plik tekstowy. Bez znaku końca linii.

Pliki w tym katalogu zawierają hasła rozwojowe (`secret`). Dla środowiska produkcyjnego nadpisz je losowymi wartościami i nie commituj do repozytorium (patrz `.gitignore` w korzeniu projektu).
